import * as path from 'path'

const dirs = {
    projectRoot: path.resolve(__dirname, '../..'),
    views: 'views',
    styles: 'styles',
    build: 'build',
    dist: 'dist',
    database: 'database'
}

export default dirs
